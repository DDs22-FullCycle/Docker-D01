#!/usr/bin/env bash

# run golang 
# docker run -it --rm --name my-running-app my-golang-app
# docker run --rm --name=golang-container -dit dds22/public:golang-fcdocker01 
docker run --rm --name=golang-container -it dds22/public:golang-fcdocker01 